

{% block heading %}
	<div class="page-heading header-text {{ banner|lower|replace({' ': '-'}) }}">
		<div class="container">
			<div class="row">
				<div class="col-lg-12">
					<h1>{{ pageName }}</h1>
					<h2></h2>
				</div>
			</div>
		</div>
	</div>
{% endblock %}

